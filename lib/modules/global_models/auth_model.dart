class AuthModel {
  int? id;
  String? nama;
  String? email;
  String? photo;
  String? roles;
  String? token;
  
  AuthModel({this.id, this.nama, this.email, this.photo, this.roles, this.token});
}